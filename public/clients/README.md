# Customer logos

Drop real, permission-cleared client logos here, keeping the same filenames
referenced in `src/data/trust.ts` (`CLIENTS`):

- de-bijenkorf.png
- picnic.png
- bidfood.png
- asa-matcha.png
- noor.png

The current files are simple name placeholders — replace each with the real
logo (transparent PNG or SVG, ~40px tall, trimmed of padding). The "trusted by"
bar on the homepage renders every entry that has a file.
